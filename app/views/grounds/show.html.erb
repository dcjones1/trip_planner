<br>
<div style="margin-left:10px">
  <h2>Your Ground Transportation</h2><br>
  <div class="justify-content-center">
    <p><strong>Departing Location:</strong><%= @ground.origin%></p>
    <p><strong>Destination:</strong> <%= @ground.destination%></p>
    <p><strong>Duration:</strong> <%= @ground.duration%></p>
    <p><strong>Mode of Transportation:</strong> <%= @ground.mode.capitalize%></p>
  </div><br>
  <h3>Directions</h3>
  <% @ground.directions.each_with_index do |direction, i|%>
  <p><%= "#{i+1}. "%> <%="#{direction}".html_safe%></p>
  <% end %>
</div>
<div class="btn-group">
<%= button_to "View Trip Details", trip_path(@ground.trip), method: :get, class: "btn btn-outline-info"%>
<%= button_to "Edit Ground Transportation", edit_ground_path(@ground), method: :get, class: "btn btn-outline-info"%>
</div>
</div>
